<UserControl x:Class="TrackPoint_Test.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrackPoint_Test.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FFE5E5E5">
        <Border Width="350" Padding="20" Background="White"
                VerticalAlignment="Center" HorizontalAlignment="Center"
                BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
            <StackPanel>

                <TextBlock Text="TrackPoint Login" FontSize="24" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Username -->
                <TextBlock Text="Username:" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" FontSize="14"/>

                <!-- Password -->
                <TextBlock Text="Password:" FontWeight="SemiBold" Margin="0,10,0,0"/>
                <PasswordBox x:Name="PasswordBox" FontSize="14"/>

                <!-- Error Message Display -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" 
                           Visibility="{Binding ErrorVisibility}" Margin="0,10,0,0"
                           TextWrapping="Wrap"/>

                <!-- Login Button -->
                <Button Content="Login" Margin="0,20,0,0"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=PasswordBox}"
                        IsDefault="True"
                        FontSize="16" FontWeight="Bold" />

            </StackPanel>
        </Border>
    </Grid>
</UserControl>